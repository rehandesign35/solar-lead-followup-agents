{
  "name": "solar-lead-followup-agents",
  "version": "0.1.0",
  "description": "Multi-agent lead qualification & follow-up system (Project 2). Supervisor + sub-agents built on LangGraph.js, sharing state via Supabase.",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --require ts-node/register --test agents/*.test.ts"
  },
  "dependencies": {
    "@langchain/langgraph": "^0.2.0",
    "@langchain/core": "^0.3.0",
    "@langchain/openai": "^0.3.0",
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.14.0",
    "ts-node": "^10.9.0",
    "@vercel/node": "^3.2.0"
  }
}
